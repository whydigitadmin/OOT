import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AirExportBlreleaseDeskWithinslaReportComponent } from './air-export-blrelease-desk-withinsla-report.component';

describe('AirExportBlreleaseDeskWithinslaReportComponent', () => {
  let component: AirExportBlreleaseDeskWithinslaReportComponent;
  let fixture: ComponentFixture<AirExportBlreleaseDeskWithinslaReportComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [AirExportBlreleaseDeskWithinslaReportComponent]
    });
    fixture = TestBed.createComponent(AirExportBlreleaseDeskWithinslaReportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
